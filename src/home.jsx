import React from 'react';

class Home extends React.Component {
    render() {
      return (
        <div>
            Calculator component will be rendered here.
        </div>
      );
    }
  }

export default Home;